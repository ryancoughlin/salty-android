package com.example.saltyoffshore.data

/**
 * Categories for global overlay layers.
 * Matches iOS OverlayCategory enum.
 */
enum class OverlayCategory(val displayName: String) {
    CONDITIONS("Conditions"),
    MAP_LAYERS("Map Layers"),
    MARKERS("Markers")
}
